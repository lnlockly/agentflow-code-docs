---
title: "Визитка (tg_profile_bundle)"
description: "AI-визитка из TG-профиля: pipeline, файлы, gotchas."
---

# Визитка (tg_profile_bundle)

> **TODO** — заполнить researcher-агентом. Стартовый skeleton.

## TL;DR

(1-2 предложения)

## Files

| Repo / path | Что делает |
|---|---|
| `agentflow-agents/src/...` | (file ref) |

## DB tables / env / API surface

(таблицы Postgres, env vars, REST endpoints)

## Failure modes

| Симптом | Лог-строка для grep | Где починить |
|---|---|---|
| `scrape_tg` fails on diploma sub-projects whose brief contains a TG @username but no explicit `username` field | `scrape_tg_requires_username_or_brief` (literal in handler) | Hit on diploma run #591 (`tg_profile_bundle` sub of #590) — brief was `"AI-визитка для @avsee4. Соскрейпь профиль..."` but the handler never extracted the @-mention. Suggested fix: regex `/@([A-Za-z0-9_]{3,32})/` over the `brief` string when `username` is not supplied; pass the first match as `username`. Handler source: `agentflow-agents/src/services/graph-executor/handlers/scrape-tg.ts` (grep for the literal error). **Backlog — not blocking diploma preemptive parallel feature.** |

## Related

- [Architecture overview](/architecture/overview)
