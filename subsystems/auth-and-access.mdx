---
title: "Auth + access"
description: "Три пути auth: x-api-key (af_live_, HMAC-SHA256), Bearer JWT, af_session cookie. Per-IP brute-force ban, platform-admin guard, project ACL, API-key rate-limit tiers."
---

# Auth + access

## TL;DR

`requireSession` middleware пробует три пути в порядке: **x-api-key** (`af_live_*` HMAC-SHA256 lookup) → **Bearer JWT** (HS256) → **af_session cookie**. После N подряд invalid auth с одного IP — 429 `too_many_auth_failures`. Platform-admin gate (`requireAdmin`) кэшит лукап на 60s. Project access: owner OR platform_admin. API-key tiers (PRO 300 RPM / FREE 60 RPM) — опциональный gate за `FF_API_KEY_TIERS=1`.

## Files

| Repo / path | Что делает |
|---|---|
| `agentflow-agents/src/middleware/auth.ts:16` | `SessionClaims` type — `{ user_id, email?, source: 'jwt'\|'api_key'\|'cookie' }`. |
| `agentflow-agents/src/middleware/auth.ts:24` | `COOKIE_NAME = 'af_session'`. |
| `agentflow-agents/src/middleware/auth.ts:25` | `INTERNAL_HEADER = 'x-agentflow-secret'` — header для internal-only routes. |
| `agentflow-agents/src/middleware/auth.ts:56` | `safeEqual` — timing-safe compare для HMAC и shared secrets. |
| `agentflow-agents/src/middleware/auth.ts:77` | `RAW_KEY_PREFIX = 'af_live_'`. |
| `agentflow-agents/src/middleware/auth.ts:86` | `hashApiKey` — HMAC-SHA256(raw_key, AGENTS_MASTER_KEY) → hex hash для DB lookup. |
| `agentflow-agents/src/middleware/auth.ts:103` | `findUserByApiKey` — `SELECT user FROM api_keys WHERE hash=? AND revoked_at IS NULL`. |
| `agentflow-agents/src/middleware/auth.ts:151` | `requireSession` — main gate. Tries x-api-key → Bearer JWT → cookie. Brute-force ban: 429 после порога. |
| `agentflow-agents/src/middleware/auth.ts:241` | `requireAdmin` — wraps requireSession + isPlatformAdmin. |
| `agentflow-agents/src/middleware/auth.ts:253` | `requireInternalSecret` — sole check для internal endpoints (compare с `INTERNAL_API_SECRET`). |
| `agentflow-agents/src/services/access.ts:26` | `isPlatformAdmin` — DB lookup с 60s in-process cache. |
| `agentflow-agents/src/services/access.ts:39` | `userCanAccessProject` — owner OR platform_admin. |
| `agentflow-agents/src/services/access.ts:56` | `userCanDispatchAgent` — analogous для agent-trigger / cron. |
| `agentflow-agents/src/middleware/api-key-rate-limit.ts:114` | `defaultLimitFor` — читает `API_KEY_RPM_PRO=300`, `API_KEY_RPM_FREE=60`. Feature flag `FF_API_KEY_TIERS=1`. Возвращает 429 `rate_limited`. |
| `agentflow-agents/src/routes/auth-magic-link.ts:206` | `POST /auth/magic-link/request` + alias `/auth/magic-link` — insert hashed nonce, send email via Resend, fall back to `dev_link` only when `ALLOW_DEV_MAGIC_LINK=1 && NODE_ENV!=production`. |
| `agentflow-agents/src/routes/auth-magic-link.ts:316` | `GET\|POST /auth/magic-link/verify` + aliases `/auth/verify` — hash incoming token, look up unused unexpired row, find-or-create user by email, mint 7-day JWT, set `af_session` cookie. |
| `agentflow-landing/src/pages/AuthEmailPage.tsx:46` | `/auth/email` page — **dual-mode** (password tab default + magic-link tab fallback). Password tab posts to `/auth/login` via `agentsPost`, hard-navigates to `redirect_to` (or `?next=`) so AuthProvider re-bootstraps with the fresh `af_session` cookie. Magic-link tab kept as fallback so any working SMTP path still works. |
| `agentflow-landing/src/pages/AuthForgotPassword.tsx` | `/auth/forgot-password` page — posts to `/auth/password-reset/request`. Always shows success state (privacy posture). |
| `agentflow-landing/src/pages/AuthResetPassword.tsx` | `/auth/reset-password?token=…` page — posts to `/auth/password-reset/confirm`. |
| `agentflow-landing/src/pages/AuthVerifyPage.tsx:36` | `/auth/verify?token=…` page — posts to `/auth/magic-link/verify` via `agentsPost`, then invalidates `ME_QUERY_KEY` and redirects to `?next=`. Reachable via inbox links (magic-link tab on `/auth/email`). |
| `agentflow-landing/src/main.tsx:322` | Router mount for `/auth/email`, `/auth/verify`, `/auth/forgot-password`, `/auth/reset-password`. |
| `agentflow-agents/src/routes/auth-password.ts:313` | `POST /auth/register` (gated by `PUBLIC_REGISTRATION_OPEN=1`). |
| `agentflow-agents/src/routes/auth-password.ts:362` | `POST /auth/login` — bcrypt verify (cost 12), constant-time compare with dummy hash on miss, mints same `af_session` JWT cookie as magic-link path. Returns `{ok, user_id, redirect_to}`. |
| `agentflow-agents/src/routes/auth-password.ts:364` | `GET /auth/me` — primary AuthProvider bootstrap. Verifies `af_session` (via `requireSession`), returns `{ok, user: {id, email, ref_code, tg_id, tg_username, is_admin, access_granted_at, joined_at}, wallet:null, balance_micro:0, invited_by:null}`. Added 2026-05-14 because `agentflow-api /me` returning 5xx silently logged users out — bootstrapping from the cookie-issuing service decouples session validity from agentflow-api uptime. Landing enriches wallet/balance/invited_by via `apiGet('/me')` afterwards, treating its failure as non-fatal. |
| `agentflow-landing/src/lib/auth.tsx:63` | `AuthProvider` queryFn — calls `agentsGet('/auth/me')` first; only treats `AgentsApiError` 401 as anonymous. Then enriches with `apiGet('/me')` for wallet/balance/invited_by; any failure there is non-fatal as long as agents authed us. |
| `agentflow-agents/src/routes/auth-password.ts:368` | `POST /auth/logout` — clears the `af_session` cookie. |
| `agentflow-agents/src/routes/auth-password.ts:476` | `POST /auth/password-reset/request` — HMAC-SHA256 hashes the reset token with `API_KEY_HMAC_SECRET`/`AUTH_JWT_SECRET`. |
| `agentflow-agents/src/routes/auth-password.ts:566` | `POST /auth/password-reset/confirm` — verifies hashed token, bcrypts new password. |
| `agentflow-agents/migrations/2026-05-11e-password-auth.sql` | Adds `users.password_hash` nullable + `auth_password_resets` table. |
| `agentflow-agents/migrations/2026-05-16d-users-deleted-at-and-slug-backfill.sql` | Adds `users.deleted_at` + backfills `users.slug = 'user_' \|\| id` for legacy NULL rows. **Side-effect:** `users.slug` is now `text NOT NULL UNIQUE` with no DEFAULT — every INSERT must supply a slug or the row rejects with `23505`/`23502`. |
| `agentflow-api/src/db/schema.ts:32` | `slug: text('slug').notNull().unique()` — drizzle column added 2026-05-17 to match the prod DDL after fresh signups began 500ing. |
| `agentflow-api/src/repos/slugs.ts` | Slug derivation per identity (`slugForTgId`, `slugForWallet`, `slugForRefCode`, `slugFromEmail`) + `withSlugRetry` wrapper that catches `23505` on `users.slug` and retries with a fresh hex suffix. |
| `agentflow-api/src/repos/users.ts:99` | Bot/internal-users insert — uses `slugForTgId(input.tg_id)`. |
| `agentflow-api/src/services/users.ts:53` | SIWE wallet signup — uses `slugForWallet(addr)`. |
| `agentflow-api/src/services/users.ts:106` | TG-link signup — uses `slugForTgId(tgId)`. |
| `agentflow-api/src/services/users.ts:149` | email/password signup — uses `slugFromEmail(email)` (deterministic local-part + random suffix). |
| `agentflow-api/src/services/users.ts:189` | `ensureAdminUser` — uses `slugForRefCode(ADMIN_REF_CODE)`. |

## Data flow

```
HTTP request → Hono middleware chain
   ↓
requireSession (middleware/auth.ts:151)
   ├─ if header x-api-key present
   │    ↓ check brute-force ban (per IP)
   │    ↓ hashApiKey(raw) → findUserByApiKey
   │    ↓ ok → claims.source='api_key' → next
   │    ↓ fail → bump IP counter, 401 invalid_api_key
   │
   ├─ else if Authorization: Bearer <jwt>
   │    ↓ verify HS256 with AUTH_JWT_SECRET
   │    ↓ ok → claims.source='jwt' → next
   │    ↓ fail → 401 jwt_invalid
   │
   ├─ else if Cookie: af_session=<jwt>
   │    ↓ verify HS256
   │    ↓ ok → claims.source='cookie' → next
   │    ↓ fail → 401 bad_session
   │
   └─ else 401 unauthenticated

Route handler reads c.get('session') → SessionClaims
   ↓ per-route ACL:
userCanAccessProject(user_id, project_id)  — access.ts:39
   ↓ project.owner_user_id === user_id  OR  isPlatformAdmin(user_id)
   ↓ deny → 403 forbidden

Optional: FF_API_KEY_TIERS=1 →
   api-key-rate-limit middleware → 429 rate_limited if RPM exceeded
```

## DB tables / env / API surface

**DB:**
- `users` — `id`, `email`, `is_platform_admin` boolean.
- `api_keys` — `{ id, user_id, hash (HMAC-SHA256 hex), prefix, label, tier?, revoked_at?, last_used_at }`.
- (см. `src/db/schema.ts`)

**Env:**
- `AGENTS_MASTER_KEY` — base64-32, используется и для api-key HMAC, и для seal/unseal.
- `AUTH_JWT_SECRET` — HS256 secret (shared с tg-mcp).
- `INTERNAL_API_SECRET` — for `/internal/*` и x-agentflow-secret header.
- `FF_API_KEY_TIERS=1` — включает per-tier RPM limits.
- `API_KEY_RPM_PRO=300`, `API_KEY_RPM_FREE=60` — лимиты.
- `BRUTE_FORCE_WINDOW_MS`, `BRUTE_FORCE_LIMIT` — за сколько подряд invalid auth блокируем IP.

**API surface:**
- `requireSession` mounted на all `/me/*`, `/admin/*` routes (`server.ts:428` mount table).
- `requireAdmin` — `/admin/*` strict.
- `requireInternalSecret` — `/internal/*` (см. `routes/internal.ts:49+`).

**Error codes (HTTP):**
- 401 `unauthenticated` — нет креденшла.
- 401 `invalid_api_key` / `jwt_invalid` / `bad_session` — auth-specific.
- 429 `too_many_auth_failures` — brute-force ban hit.
- 429 `rate_limited` — RPM exceeded.
- 500 `jwt_not_configured` — `AUTH_JWT_SECRET` отсутствует в env.
- 403 `forbidden` — ACL deny.

## Failure modes

| Симптом | Лог-строка для grep | Где починить |
|---|---|---|
| /auth/email молча редиректит обратно на /login | (visual) `<Navigate to="/login" replace />` в `main.tsx` для `/auth/email` | `agentflow-landing/src/main.tsx:322` — mount `<AuthEmailPage />` напрямую. |
| /auth/email shows only magic-link tab (no password input) | `mode === 'link'` stuck without segmented control | `AuthEmailPage.tsx:46` — default `mode` state should be `'password'`; tab toggle wired to `setMode`. |
| Login returns 401 even with right creds | `invalid_credentials` from `POST /auth/login` | `agentflow-agents/src/routes/auth-password.ts:362` — `password_hash` is NULL for users created via Telegram OAuth or magic-link. Set one with `/auth/password-reset/confirm` or via admin bootstrap. |
| Password-reset email never arrives | `[auth-password] reset_mail_failed: mailer_not_configured` | Wire `RESEND_API_KEY` в `agentflow-agents` Secret. Until then `/auth/forgot-password` silently no-ops; only Telegram login or pre-set passwords work. |
| User has `password_hash IS NULL` and cannot log in (mailer dead) | `SELECT password_hash FROM users WHERE email=$1` → NULL | Bootstrap via SQL: `UPDATE users SET password_hash = '$2b$12$…' WHERE email=$1;` then notify user to reset on first login. Use `node -e "console.log(require('bcrypt').hashSync('temp_pw', 12))"`. |
| Лавинообразные 401 от одного IP | `too_many_auth_failures` | `middleware/auth.ts:151` (brute-force gate) |
| Все api-keys внезапно отвергаются | `jwt_not_configured` или `AGENTS_MASTER_KEY missing` | env config; `middleware/auth.ts:86` |
| Platform admin не виден | `is_platform_admin_cache_stale` | `services/access.ts:26` — invalidate / restart pod |
| Owner не может открыть свой project | `acl_owner_mismatch` | `services/access.ts:39` (userCanAccessProject) |
| 429 rate_limited при низкой нагрузке | `api_key_rpm_exceeded` | `middleware/api-key-rate-limit.ts:114` — поднять `API_KEY_RPM_*` |
| Internal endpoint не пускает (свой сервис) | `internal_secret_mismatch` | check `INTERNAL_API_SECRET` aligned между API-pod и runtime-supervisor / tg-mcp |
| Successful login → /my/agents всё равно показывает «Нужен вход / Войти через Telegram» | curl `/_api/me` returns 503 while `/_agents/auth/me` returns 200 | `agentflow-api` service down. Now non-fatal: `AuthProvider` (`agentflow-landing/src/lib/auth.tsx:63`) bootstraps from `agentflow-agents` `/auth/me` first; agentflow-api `/me` is only an enrichment call. If both fail, user is anonymous. Fix root cause by reviving the `agentflow-api` deployment. |
| `POST /internal/users` 500 for every new TG `/start`; all SIWE/email/TG-link signups also broken | `null value in column "slug" of relation "users" violates not-null constraint` | Drizzle schema in `agentflow-api/src/db/schema.ts` was missing the `slug` column after migration `2026-05-16d-users-deleted-at-and-slug-backfill.sql` added `users.slug text NOT NULL UNIQUE` to prod. Fixed 2026-05-17 by adding the column to the schema and routing every `db.insert(users)` through `src/repos/slugs.ts::withSlugRetry` with a per-identity derivation (`slugForTgId`, `slugForWallet`, `slugFromEmail`, `slugForRefCode`). |

## Related

- [Architecture overview](/architecture/overview)
- [LLM router](/subsystems/llm-router)
