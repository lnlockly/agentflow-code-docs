---
title: "Contact Farm"
description: "Owner-scoped contact-messaging farm — user_farms + farm_stats, /me/farm/* endpoints, FarmPage and the ProductsHub farm card."
---

# Contact Farm

## TL;DR

Per-user "contact farm" feature: a connected Telegram account auto-sends
one referral-link message per day to its contacts. Backend tables and
endpoints shipped 2026-05-15; the daily worker that actually sends the
messages is not yet built — see Backlog.

The farm's `status` column is the canonical source for the
`farm_activated` funnel stage in [Funnel analytics](/subsystems/funnel-analytics)
and the `farm_activated` state in [Reminders v2](/subsystems/reminders-v2).

## Files

| Path | Role |
|---|---|
| `agentflow-agents/src/db/schema.ts:2479` | `userFarms` Drizzle table — status / message_template / send_hour |
| `agentflow-agents/src/db/schema.ts:2506` | `farmStats` Drizzle table — counters per user |
| `agentflow-agents/src/db/migrations/0064_farm_tables.sql` | Migration creating both tables |
| `agentflow-agents/src/routes/farm.ts:19` | Hono `app` for `/me/farm/*` |
| `agentflow-agents/src/routes/farm.ts:40` | `GET /me/farm/stats` — never 404s; returns `EMPTY_STATS` for unknown users |
| `agentflow-agents/src/routes/farm.ts:73` | `POST /me/farm/start` — upsert with status='active' |
| `agentflow-agents/src/routes/farm.ts:98` | `POST /me/farm/pause` — set status='paused' |
| `agentflow-agents/src/routes/farm.ts:122` | `POST /me/farm/toggle` — flip active↔paused |
| `agentflow-agents/src/server.ts:85` | Imports `farmApp` |
| `agentflow-agents/src/server.ts:515` | Mounts `farmApp` at `/` |
| `agentflow-landing/src/pages/FarmPage.tsx:170` | `/farm` route — dashboard + connect-TG block |
| `agentflow-landing/src/pages/ProductsHub.tsx:107` | Farm product card on `/products` (live `contacts` metric) |
| `agentflow-landing/src/pages/ProjectsDashboard.tsx:629` | `FarmBanner` widget on `/projects` |

## Data flow

```
                  user clicks "Запустить ферму"
                              │
                              ▼
                  POST /me/farm/start  (session auth)
                              │
                              ▼
                  upsert user_farms { user_id, status:'active' }
                  upsert farm_stats { user_id, … defaults }
                              │
                              ▼
                  GET /me/farm/stats  ─► dashboards (FarmPage, ProductsHub
                                          farm card, UserStatsStrip,
                                          ProjectsDashboard FarmBanner)

  [BACKLOG]  daily worker (cron, NOT YET BUILT):
    SELECT users WHERE user_farms.status='active' AND
           (send_hour reached today AND last_run_at < today)
    → for each user: pick contacts → send 1 TG msg with ref link
    → UPDATE farm_stats SET sent_today, sent_total, last_run_at
    → INSERT user_events (kind='farm_activated') on first send
```

The funnel's `farm_activated` stage in `/admin/funnel-stats` is sourced
directly from `COUNT(*) FROM user_farms WHERE status='active'` (see
`stats.ts:133`), not from the events table — activation is observable
the moment the user clicks start, before any send has fired.

## DB / Env / API

- Tables:
  - `user_farms (id, user_id UNIQUE, status, message_template, send_hour, created_at, updated_at)`
  - `farm_stats (id, user_id UNIQUE, contacts_count, sent_total, sent_today, registrations, flow_earned, last_run_at, updated_at)`
- Indexes: `user_farms_user_id_idx` UNIQUE, `farm_stats_user_id_idx` UNIQUE
- Status enum (string-typed): `'inactive' | 'active' | 'paused'`
- `send_hour`: UTC hour 0-23 at which the daily worker should fire (default 10)
- Routes (all `requireSession`, user_id = session.sub):
  - `GET /me/farm/stats` — returns `{ ok, farm: { status, message_template, send_hour, contacts_count, sent_total, sent_today, registrations, flow_earned, last_run_at } }`
  - `POST /me/farm/start`
  - `POST /me/farm/pause`
  - `POST /me/farm/toggle`

## Failure modes

| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| User without any farm record gets a response | `EMPTY_STATS` returned (status='inactive', all zeros) | By design — endpoint never 404s. Frontend renders the connect-TG CTA |
| Farm stage stuck at 0 in admin funnel | n/a | Confirm `user_farms` migration 0064 applied, then check `SELECT count(*) FROM user_farms WHERE status='active'` directly |
| `FarmPage` shows mock data | `MOCK_STATS` constant in `FarmPage.tsx:37` | Frontend still uses mock-on-fetch-failure — verify `/me/farm/stats` reachable from browser |
| `ProductsHub` farm card has no live count | `apiGet<FarmStatsLite>('/me/farm/stats')` in `ProductsHub.tsx:135` returned error | Card hides the metric silently — check auth state, agentsBaseUrl in DevTools |
| User exists but `toggle` does nothing | Toggle path explicitly only flips active↔paused | If status='inactive' (never started), call `/me/farm/start` first |

## Backlog

- **Daily send worker** — the cron that reads `user_farms.status='active'` and actually sends one TG message per user per day. Until it lands, `sent_today` / `sent_total` / `last_run_at` stay at zero and `registrations` is only credited when an outside referral signup fires.
- **Contact pool ingestion** — UI for the user to import their contacts list. Currently `contacts_count` is admin-writable only.

## Related

- [Funnel analytics](/subsystems/funnel-analytics) — `farm_activated` stage sourced from this table
- [Reminders v2](/subsystems/reminders-v2) — `farm_activated` user state matches `user_farms.status='active'`
- [Projects Dashboard](/subsystems/projects-dashboard) — FarmBanner widget consumes `/me/farm/stats`
- [Products Hub](/subsystems/products-hub) — Farm card consumes `/me/farm/stats`
