---
title: "Graph executor — node handlers"
description: "Typed handler registry: every node id in agent_boilerplates.workflow_schema maps to a handler that wraps an existing service or delegates to a coder agent with a tight brief."
---

# Graph executor — node handlers

## TL;DR

The walker resolves each `WorkflowNode.id` to a typed handler via
`defaultRegistry.resolve(kind)`. 50+ unique node ids ship across 16
seeded boilerplates (5 originals + diploma composite + 6 TG-template
extras from PR #196); each has a handler under
`src/services/graph-executor/handlers/<group>.ts`. Deterministic work
(deploy, scrape, smoke-test, request_token, setup_bot, link_account)
wraps an existing service in ≤20 LOC. Generative work (pick_theme,
scaffold_*, *_flow) goes to a per-node coder-delegate with a tight
`OUTPUT_EXPECTED` envelope so the walker can validate the result.

The registry also resolves `kind:variant` suffixes (e.g.
`tg.send_text:deliver`) to the base handler, so the diploma DAG can
label per-edge variants without duplicate registrations.

Contract: [`architecture/graph-executor.mdx`](/architecture/graph-executor).

## Boilerplate × node id matrix

Source: prod `agent_boilerplates.workflow_schema` (10 rows, 5 seeded).

| id | kind | name | nodes |
|---|---|---|---|
| 3  | spa | vite-react-ts | (empty — stub) |
| 4  | api | fastapi-best-practices | (empty — stub) |
| 5  | landing | vite-react-tailwind-min | (empty — stub) |
| 6  | mini_app | mini-app-fullstack | `scaffold_vite, tg_webapp_init, routes, deploy` |
| 8  | landing | vite-react-tailwind-shadcn | (empty — stub) |
| 12 | tg_bot | tg-bot-full | `request_token, scaffold_aiogram, smoke_test, self_heal_loop, deploy` |
| 13 | tg_bot | tg-bot-minimal | (empty — stub) |
| 14 | spa | webapp-full | `scaffold_nest, scaffold_prisma, jwt_auth, billing_stripe, multi_tenant, admin_panel, deploy` |
| 15 | landing | landing-full | `pick_theme, generate_sections, seo_optimize, deploy_caddy` |
| 18 | tg_profile_bundle | tg-profile-bundle | `scrape_tg, render_landing, deploy_landing, scaffold_bot, wire_designapi, deploy_bot, booking_flow` |
| 19 | tg_bot | tg-subscription-gate | `setup_bot, fetch_target_channel, register_callback, check_subscription, grant_or_deny` |
| 20 | tg_bot | tg-voice-transcriber | `setup_bot, receive_voice, transcribe_whisper, format_text, reply_to_user` |
| 21 | tg_bot | tg-join-request-handler | `setup_bot, register_join_listener, store_metadata, notify_owner, auto_approve_or_queue` |
| 22 | tg_bot | tg-content-crossposter | `setup_bot, poll_source_channel, ai_summarize, format_for_platform, post_to_linkedin_or_x` |
| 23 | tg_farm | tg-cold-mailer | `link_account, import_leads, personalize_template, throttled_send, log_replies` |
| 24 | tg_farm | tg-ai-shiller | `link_account, set_keywords, poll_chats, ai_compose_reply, send_reply` |
| diploma | tg_bot | composite DAG | `tg.get_user, tg.send_text, tg.wait_reply, llm.classify_intent, project.create_sub, project.wait_done, tg_lead_states.update` |

Empty `workflow_schema` rows ship with a stub graph; the walker falls
through to maestro for them.

## Handler registry

| node id | file | wraps | retry (max / backoff / idempotent) | timeout |
|---|---|---|---|---|
| `deploy` | `handlers/shared.ts` | `runtime-supervisor.deploy(projectId, ownerUserId, {source:'always_on'})` | 2 / 5s / true | 600s |
| `deploy_caddy` | `handlers/landing.ts` | `build-node-wrappers.runBuildCaddyRoute` → code-exec `expose_public` | 2 / 5s / true | 120s |
| `deploy_landing` | `handlers/tg-profile-bundle.ts` | `runtime-supervisor.deploy({source:'flag_gated'})` | 2 / 5s / true | 600s |
| `deploy_bot` | `handlers/tg-profile-bundle.ts` | `runtime-supervisor.deploy({source:'always_on'})` | 2 / 5s / true | 600s |
| `scrape_tg` | `handlers/tg-profile-bundle.ts` | `build-node-wrappers.runBuildScrapeTgProfile` → `auto-scrape.scrapeOneTarget` / `autoScrapeFromBrief` | 2 / 5s / true | 240s |
| `request_token` | `handlers/tg-bot.ts` | `auto-bot.autoCreateTelegramBot` (pool → BotFather fallback) | 2 / 5s / true | 180s |
| `smoke_test` | `handlers/tg-bot.ts` | `bot-auto-tester.runBotSmokeTest` | 1 / 10s / true | 240s |
| `self_heal_loop` | `handlers/tg-bot.ts` | `self-heal-bot.createSmokeFixTask` (reads `ctx.steps['smoke_test'].output.report`) | 0 / 0s / false | 60s |
| `wire_designapi` | `handlers/tg-profile-bundle.ts` | `build-node-wrappers.runBuildWireDesignapi` → designapi-helper agent | 1 / 5s / false | 90s |
| `pick_theme` | `handlers/landing.ts` | coder-landing delegate (writes `/workspace/.runtime/theme.json`) | 1 / 3s / false | 600s |
| `generate_sections` | `handlers/landing.ts` | coder-landing delegate (9 section TSX components) | 1 / 3s / false | 600s |
| `seo_optimize` | `handlers/landing.ts` | coder-landing delegate (meta + sitemap.xml + robots.txt) | 1 / 3s / false | 600s |
| `render_landing` | `handlers/landing.ts` | coder-landing delegate (renders from `ctx.steps.scrape_tg.output`) | 1 / 3s / false | 600s |
| `scaffold_aiogram` | `handlers/tg-bot.ts` | coder-tg-bot delegate (bot.py + Procfile) | 1 / 3s / false | 600s |
| `scaffold_bot` | `handlers/tg-profile-bundle.ts` | coder-tg-bot delegate (in-place bot inside the visitka bundle) | 1 / 3s / false | 600s |
| `booking_flow` | `handlers/tg-profile-bundle.ts` | coder-backend delegate (SQLite `bookings` + TG handler) | 1 / 3s / false | 600s |
| `scaffold_vite` | `handlers/mini-app.ts` | coder-spa delegate (Vite + React + @twa-dev/sdk) | 1 / 3s / false | 600s |
| `tg_webapp_init` | `handlers/mini-app.ts` | coder-spa delegate (initData validation + tma-auth route) | 1 / 3s / false | 600s |
| `routes` | `handlers/mini-app.ts` | coder-spa delegate (react-router + TG BackButton/MainButton) | 1 / 3s / false | 600s |
| `scaffold_nest` | `handlers/spa.ts` | coder-backend delegate (NestJS 11 skeleton) | 1 / 3s / false | 600s |
| `scaffold_prisma` | `handlers/spa.ts` | coder-backend delegate (Prisma 6 + User/Org/Membership) | 1 / 3s / false | 600s |
| `jwt_auth` | `handlers/spa.ts` | coder-backend delegate (passport-jwt + /auth routes) | 1 / 3s / false | 600s |
| `billing_stripe` | `handlers/spa.ts` | coder-backend delegate (Stripe Checkout + webhook) | 1 / 3s / false | 600s |
| `multi_tenant` | `handlers/spa.ts` | coder-backend delegate (X-Org-Id + Membership guard) | 1 / 3s / false | 600s |
| `admin_panel` | `handlers/spa.ts` | coder-spa delegate (React admin under admin-frontend/) | 1 / 3s / false | 600s |
| `tg.get_user` | `handlers/tg.get_user.ts` | MCP `telegram/get_user` (peer lookup → username/user_id) | 2 / 3s / true | 60s |
| `tg.send_text` | `handlers/tg-composite.ts` | MCP `telegram/send_message`, peer fallback to upstream `tg.get_user.username` | 1 / 0s / false | 60s |
| `tg.wait_reply` | `handlers/tg-composite.ts` | MCP `telegram/get_history` cursor poll, emits `node_waiting` | 1 / 0s / true | 1800s |
| `llm.classify_intent` | `handlers/tg-composite.ts` | `ctx.llm(ModelType.TEXT_LARGE)` → strict JSON intent | 1 / 0s / true | 60s |
| `project.create_sub` | `handlers/project.ts` | `pipelines.startApproval` for child project | 1 / 3s / false | 60s |
| `project.wait_done` | `handlers/project.wait_done.ts` | polls `projects.status` until terminal | 1 / 3s / true | 1800s |
| `tg_lead_states.update` | `handlers/tg_lead_states.update.ts` | upserts row in `tg_lead_states` (idempotent) | 2 / 3s / true | 30s |
| `setup_bot` | `handlers/tg-bot-extras.ts` | `auto-bot.autoCreateTelegramBot` (idempotent, shared by 4 tg_bot extras) | 2 / 5s / true | 180s |
| `fetch_target_channel` / `register_callback` / `check_subscription` / `grant_or_deny` | `handlers/tg-bot-extras.ts` | coder-tg-bot delegate (subscription-gate flow) | 1 / 3s / false | 600s |
| `receive_voice` / `transcribe_whisper` / `format_text` / `reply_to_user` | `handlers/tg-bot-extras.ts` | coder-tg-bot delegate (voice-transcriber flow) | 1 / 3s / false | 600s |
| `register_join_listener` / `store_metadata` / `notify_owner` / `auto_approve_or_queue` | `handlers/tg-bot-extras.ts` | coder-tg-bot delegate (join-request flow) | 1 / 3s / false | 600s |
| `poll_source_channel` / `ai_summarize` / `format_for_platform` / `post_to_linkedin_or_x` | `handlers/tg-bot-extras.ts` | coder-tg-bot delegate (content-crossposter flow) | 1 / 3s / false | 600s |
| `link_account` | `handlers/tg-marketing.ts` | Telethon session bind (idempotent, branches on `ctx.kind` via `PROJECT_KIND` brief hint, shared by 2 tg_farm boilerplates) | 2 / 5s / true | 180s |
| `import_leads` / `personalize_template` / `throttled_send` / `log_replies` | `handlers/tg-marketing.ts` | coder-tg-farm delegate (cold-mailer flow) | 1 / 3s / false | 600s |
| `set_keywords` / `poll_chats` / `ai_compose_reply` / `send_reply` | `handlers/tg-marketing.ts` | coder-tg-farm delegate (ai-shiller flow) | 1 / 3s / false | 600s |

Coder-fallback (`kind: '__coder__'`,
[`graph-executor/coder-fallback.ts:39`](https://github.com/lnlockly/agentflow-agents/blob/main/src/services/graph-executor/coder-fallback.ts#L39))
fires only for unknown node ids — registered handlers above resolve
without falling back. The coverage gate is enforced by
[`tests/unit/graph-executor/handlers/registry-coverage.test.ts`](https://github.com/lnlockly/agentflow-agents/blob/main/tests/unit/graph-executor/handlers/registry-coverage.test.ts).

## Files

| Path | Role |
|---|---|
| `agentflow-agents/src/services/graph-executor/handlers/index.ts` | Registration entry point — `import './<group>.js'` for each handler file (side-effect imports trigger `defaultRegistry.register(...)`) |
| `agentflow-agents/src/services/graph-executor/handlers/shared.ts` | `deploy` (one handler covers tg_bot/spa/mini_app deploy) |
| `agentflow-agents/src/services/graph-executor/handlers/landing.ts` | `pick_theme, generate_sections, seo_optimize, render_landing, deploy_caddy` |
| `agentflow-agents/src/services/graph-executor/handlers/tg-bot.ts` | `request_token, scaffold_aiogram, smoke_test, self_heal_loop` |
| `agentflow-agents/src/services/graph-executor/handlers/tg-profile-bundle.ts` | `scrape_tg, deploy_landing, scaffold_bot, wire_designapi, deploy_bot, booking_flow` |
| `agentflow-agents/src/services/graph-executor/handlers/mini-app.ts` | `scaffold_vite, tg_webapp_init, routes` |
| `agentflow-agents/src/services/graph-executor/handlers/spa.ts` | `scaffold_nest, scaffold_prisma, jwt_auth, billing_stripe, multi_tenant, admin_panel` |
| `agentflow-agents/src/services/graph-executor/handlers/tg-composite.ts` | `tg.send_text, tg.wait_reply, llm.classify_intent` (diploma composite primitives) |
| `agentflow-agents/src/services/graph-executor/handlers/tg-bot-extras.ts` | `setup_bot` + 16 coder-delegates for the 4 new tg_bot boilerplates |
| `agentflow-agents/src/services/graph-executor/handlers/tg-marketing.ts` | `link_account` + 8 coder-delegates for the 2 tg_farm boilerplates |
| `agentflow-agents/src/services/graph-executor/handlers/tg.get_user.ts` | `tg.get_user` (MCP peer lookup) |
| `agentflow-agents/src/services/graph-executor/handlers/project.ts` | `project.create_sub` (child pipeline launch) |
| `agentflow-agents/src/services/graph-executor/handlers/project.wait_done.ts` | `project.wait_done` (terminal-status poll) |
| `agentflow-agents/src/services/graph-executor/handlers/tg_lead_states.update.ts` | `tg_lead_states.update` (lead-state upsert) |
| `agentflow-agents/tests/unit/graph-executor/handlers/registry-coverage.test.ts` | Asserts every seeded node id resolves without falling back |
| `agentflow-agents/tests/unit/graph-executor/handlers/coder-delegate.test.ts` | Asserts brief envelope shape per coder-delegate handler |
| `agentflow-agents/tests/unit/graph-executor/handlers/deterministic.test.ts` | Asserts retry policy + source-level delegation for deterministic handlers |

## Data flow

```
WorkflowSchema (from agent_boilerplates row)
        │
        ▼
walker.run({ projectId, workflowSchema })
        │
        ▼ Kahn topo sort, parallel where in_edges = 0
NodeKind ── resolveHandler(kind) ──► defaultRegistry.resolve
                                          │
        ┌─────────────────────────────────┴──────────────────────────────────┐
        ▼                                                                    ▼
deterministic handler                                           coder-delegate handler
  (deploy, scrape_tg, smoke_test,                                 (pick_theme, scaffold_*,
   request_token, deploy_caddy, …)                                 booking_flow, …)
        │                                                                    │
        ▼                                                                    ▼
wraps existing service                                          ctx.coderInvoke({
(runtime-supervisor.deploy,                                       slug, brief,
 auto-bot.autoCreateTelegramBot,                                  expects: [files...],
 bot-auto-tester.runBotSmokeTest,                                 timeoutMs })
 build-node-wrappers.run*)                                                  │
        │                                                                    ▼
        │                                                          coder-tg-bot / coder-landing /
        ▼                                                          coder-spa / coder-backend
output validated against handler.outputSchema (zod)
        │
        ▼
ctx.emit({ kind: 'node_finished', payload: { output } })
```

## DB / Env / API

- Tables: none new — uses `project_events`, `projects`, `project_secrets`, `pipeline_runs` view.
- Env:
  - `BOT_TOKEN_POOL` — read inside `request_token` → `auto-bot.autoCreateTelegramBot` only. Handlers never touch process.env directly.
  - `FF_AUTO_BOT_CREATE_BOTFATHER` — same.
  - `RUNTIME_SUPERVISOR_ENABLED` — gates the `flag_gated` path used by `deploy_landing`.
- Routes: none new. Events stream via `GET /me/pipelines/:id/events` SSE (consumed by `CanvasLive`).
- MCP tools called by handlers: `code-exec.expose_public` (deploy_caddy), `code-exec.exec` (transitively via wrappers).

## Failure modes

| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| Node falls through to coder-fallback in prod | `[graph-executor] handler missing for kind=<id>, delegating to coder-agent` | Add handler under `handlers/<group>.ts` and re-run `tests/unit/graph-executor/handlers/registry-coverage.test.ts` |
| `request_token` returns assigned=true but bot has no `BOT_TOKEN` secret | `[auto-bot] pool empty and FF_AUTO_BOT_CREATE_BOTFATHER off, skipping` | Refill pool OR set the flag; see [bot-pool](/architecture/bot-pool) |
| `smoke_test` output has `report.skipped` | `bot_smoke_skipped` in `project_events` | Tester unavailable (no TG account). Configure a tester account; not a handler bug |
| `self_heal_loop` returns task_created=false skipped=`no_smoke_report_in_steps` | `no_smoke_report_in_steps` (literal in handler) | Upstream `smoke_test` didn't run before `self_heal_loop` — fix the edge in `agent_boilerplates.workflow_schema` |
| `scrape_tg` rejects with `scrape_tg_requires_username_or_brief` | grep handler source | Node config must include `username` or `brief` — fix the seed |
| Coder reply missing `files_written` array | handler returns `files_written: []` and walker per-kind policy decides | If kind is `fail-fast` on this node, brief was too broad — tighten `output_expected` in the registration |
| Duplicate handler registration on boot | `duplicate_handler:<kind>` thrown at module load | Two registrations for one `kind` — remove the second one |

## Related

- [Graph executor (contract & spec)](/architecture/graph-executor)
- [Bot pool & auto-assign](/architecture/bot-pool) — `request_token` handler
- [Визитка (tg_profile_bundle)](/architecture/visitka-tg-profile) — `scrape_tg + render_landing + scaffold_bot + booking_flow` chain
- [Kinds](/architecture/kinds) — per-kind DAG to handler map
- [Runtime supervisor](/subsystems/runtime-supervisor) — `deploy*` family
