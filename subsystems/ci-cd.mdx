---
title: "CI/CD"
description: "PR-time and push-to-main GitHub Actions across agentflow-agents and agentflow-landing."
description: "Pipelines that build, test, and deploy AgentFlow frontend and backend."
---

# CI/CD

## TL;DR
Two repos, two pipelines each:

- `agentflow-agents` — `pr-check.yml` runs `typecheck | build | test` in parallel on every PR (self-hosted runner). `deploy.yml` builds the GHCR image and rolls k3s on push to `main`.
- `agentflow-landing` — `e2e.yml` runs a Playwright smoke against `agentflow.website` on every PR; `deploy.yml` builds + rolls on push to `main`.

Branch protection on `agentflow-agents/main` requires `typecheck`, `build`, `test` status checks. Apply via `scripts/setup-branch-protection.sh`.

## Files (with line refs)

### agentflow-agents
| Path | Role |
|---|---|
| `agentflow-agents/.github/workflows/pr-check.yml:1` | PR-time gates (typecheck, build, test). Parallel jobs, `cancel-in-progress` concurrency keyed by PR number. |
| `agentflow-agents/.github/workflows/deploy.yml:1` | Push-to-main builder + deployer (self-hosted runner). |
| `agentflow-agents/scripts/setup-branch-protection.sh:1` | One-shot `gh api` call requiring the three pr-check status contexts. |
| `agentflow-agents/package.json:13` | `typecheck`, `build`, `test` npm scripts the gates call. |

### agentflow-landing
GitHub Actions runs build+deploy on push to `main` for both `agentflow-agents`
and `agentflow-landing`. On `pull_request`, `agentflow-landing` runs a
Playwright E2E smoke against prod `agentflow.website` to catch P0
regressions (auth, autonomous-goal POST, NaN cards) before merge.
`agentflow-agents` PRs run `typecheck` + `build` (blocking) and a
`node --test` job over `tests/` (non-blocking, `continue-on-error: true`).

## Files (with line refs)
| Path | Role |
|---|---|
| `agentflow-landing/.github/workflows/deploy.yml:1` | Build image + rollout to k3s on push to main |
| `agentflow-landing/.github/workflows/e2e.yml:1` | Playwright smoke on every PR |
| `agentflow-landing/playwright.config.ts:1` | Smoke config — Chrome only, `agentflow.website` base URL |
| `agentflow-landing/tests/e2e/autonomous-flow.spec.ts:1` | Spec: login → device → Autonomous tab → set goal |
| `agentflow-landing/tests/e2e/__helpers/anti-nan.ts:1` | `assertNoNaN(page)` — fails if DOM has NaN/undefined/null/Invalid Date |

## Data flow

### agentflow-agents PR-check
```
developer git push <feature>
   ▼
GitHub PR ─────► pr-check.yml
                   ├── typecheck   (npm run typecheck = tsc --noEmit)
                   ├── build       (npm run build, uploads dist/ artifact 5d)
                   └── test        (npm test = node --test tests/**/*.test.ts; self-skips when absent)
                                  ▼
                                PASS → eligible for squash-merge
                                FAIL → branch protection blocks merge
```

### agentflow-landing E2E
```
PR opened
  → GH Actions e2e-smoke job (ubuntu-latest, 15min timeout)
     → npm ci → npx playwright install chromium → npm run e2e:smoke
        → goto agentflow.website/login → /cabinet → /cabinet/devices
        → click "Автономно" tab → POST autonomous/goals → assertNoNaN(page)
| `agentflow-landing/playwright.auth-config.ts:1` | Older 12-journey suite — kept for token-based runs |
| `agentflow-agents/.github/workflows/pr-check.yml:67` | `test` job — runs `npm test` over all of `tests/` (non-blocking) |
| `agentflow-agents/package.json:18` | `test` script — `node --import tsx --test "tests/*.test.ts" "tests/**/*.test.ts"` |
| `agentflow-agents/.github/workflows/deploy.yml:363` | `Apply manifest + rollout` step — `kubectl apply` + `set image` both deploys, then `rollout status … --timeout=8m` (raised from 3m, 2026-05-29) under `set -e` |

## agentflow-agents test glob (the `**` quirk)

`npm test` runs via `sh -c`, where `sh` has no `globstar`, so an unquoted
`tests/**/*.test.ts` collapses `**` to a single `*` — it matches nested
`tests/<dir>/*.test.ts` but silently DROPS top-level `tests/*.test.ts`. For a
long time ~109 top-level suites never ran in CI (and the `test` job is
`continue-on-error: true`, so nobody noticed). Real totals jumped 3042 → 4434
tests once fixed.

Fix: quote BOTH patterns so the shell passes them literally to node, whose
`--test` glob handles `**` correctly and de-dupes overlapping file args:

```
"test": "node --import tsx --test \"tests/*.test.ts\" \"tests/**/*.test.ts\""
```

`pr-check.yml` `detect_tests` counts with `find tests -name '*.test.ts'` for the
same reason.

### Remaining failure buckets (2026-05-29, after dead-test cleanup)

The 4 dead graph-executor test files were deleted (they only exercised the
unwired executor — registry coverage of seeded boilerplate node ids, deterministic
handler retry contracts, coder-delegate brief envelopes, `substituteTemplates`):
`tests/unit/graph-executor/handlers/{coder-delegate,deterministic,registry-coverage}.test.ts`
and `tests/unit/graph-executor/templates.test.ts`. The graph-executor SOURCE stays
(still referenced by `src/services/projects.ts` + `src/routes/me-graph-prompts.ts`);
removal is Phase 3 / P8.

Full local run (no migrated DB): 4448 tests, 30 fail, 125 skip. Buckets:

| Bucket | Files | Fix path |
|---|---|---|
| DB-integration (need migrated Postgres in CI) | `courses` (2), `self-mcp` (2), `teams` (1), `teams-recurring` (1), `usage` (1), `character.snapshot` (1) | provision + migrate a Postgres service in the `test` job |
| missing committed artifact | `mautrix-discord-config` (10) | commit `k8s/mautrix-discord-config.yaml` + `k8s/mautrix-discord.yaml` to main |
| preview-gate / reconciler logic drift | `services/pool-worker/preview-gate` (8), `unit/pingers/preview-reconciler-watchdog` (1) | separate audit of `checkPreviewGate` strict/healthPath/backend-health branches + cool-down skip window |
| known / expected | `unit/request-platform-resources` (2) | `FF_PLATFORM_AGENT_DRIVEN` / `FF_PLATFORM_RESOURCES` guards |
| flaky under concurrency | `unit/graph-executor/walker` (1) | `parallel roots fan-in` passes in isolation, fails under full-suite timing |

The `test` job stays non-blocking until the DB-in-CI item lands. Once the only
reds left are DB-integration + the 2 known `request-platform-resources`, flip
`continue-on-error: true` to blocking.

## Data flow

```
PR opened
  → GH Actions e2e-smoke job (ubuntu-latest, 15min timeout)
     → npm ci
     → npx playwright install chromium
     → npm run e2e:smoke (= playwright test --repeat-each=2)
        → goto agentflow.website/login → fill creds → /cabinet
        → goto /cabinet/devices → click first device
        → click "Автономно" tab
        → if "Поставить цель" CTA visible → fill form → POST autonomous/goals
        → assertNoNaN(page) on Autonomous tab DOM
     → on failure: upload playwright-report/ + test-results/ artifacts
```

## DB / Env / API

### agentflow-agents
- No env required for `pr-check.yml` (runs without secrets).
- `deploy.yml` reads `GITHUB_TOKEN`, `KUBECONFIG`, registry creds (configured at runner level).
- Required status checks on `main`: `typecheck`, `build`, `test`.

### agentflow-landing
- Env (CI):
  - `E2E_BASE_URL` — defaults `https://agentflow.website`.
  - `E2E_OWNER_EMAIL` / `E2E_OWNER_PASSWORD` — GH secret. Falls back to
    `avsee4@agentflow.website` / brief-provided password.
  - `CI=true` — enables `[github]` reporter + `retries: 1`.
- Routes hit by the smoke:
  - `POST /login` (frontend form post).
  - `GET /cabinet/devices`.
  - `POST /me/devices/:device_id/autonomous/goals` — return must be < 300.
- Cleanup: goals named `[e2e] smoke <ts>` so owner can filter.

## Failure modes

### agentflow-agents PR-check
| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| PR merged despite red CI | branch protection missing status checks | run `scripts/setup-branch-protection.sh` |
| typecheck job red | `error TS2322` (or any `error TS`) in workflow log | fix the type error in the PR |
| build job red | `error TS` or `tsc` non-zero exit | local repro `npm run build` |
| test job skipped on valid PR | `No tests/**/*.test.ts found — skipping.` | check `hashFiles('tests/**/*.test.ts')` — tests dir must exist |
| `npm ci` slow | `Cache not found for keys` in setup-node step | confirm `package-lock.json` unchanged from previous PR; otherwise expected |
| Multiple runs queued on same PR | duplicated job rows | `concurrency.cancel-in-progress: true` cancels prior runs on push — expected |
| Jobs never start | `The job was not started because recent account payments have failed` | org billing blocked — workflow uses self-hosted `ff-france-runner`; verify runner online via `gh api repos/lnlockly/agentflow-agents/actions/runners` |

### agentflow-landing E2E
| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| Goal create returns 500 | `create-goal response was 500` | `agentflow-agents/src/routes/autonomous.ts` deadline parser |
| Card renders NaN | `assertNoNaN: rendered DOM contains [NaN` | `agentflow-landing/src/pages/AutonomousTab.tsx` formatters |
| Login redirect broken | `Test timeout … waiting for navigation to /cabinet` | `agentflow-landing/src/pages/LoginPage.tsx` redirect handler |
| Autonomous tab missing | `expected locator … "Автономно" to be visible` | `agentflow-landing/src/pages/DevicePage.tsx` tab registration |
| Prod under deploy | Flaky on first run, green on retry | `retries: 1` covers; if both fail, investigate `agentflow.website` |

## Related
- [Autonomous goals subsystem](./autonomous-goals)
- [Runtime supervisor](./runtime-supervisor)
- [Projects create flow](./projects-create-flow)
| agentflow-agents deploy goes RED but the image still rolled (pods healthy 2/2) | `error: timed out waiting for the condition` on the `Apply manifest + rollout` step | `agentflow-agents/.github/workflows/deploy.yml:363` — `set image` already happened before the wait, so the deploy DID land; the `rollout status` wait timed out because a surge pod couldn't SCHEDULE fast on the CPU-tight node. Timeout raised 3m→8m (2026-05-29). Do NOT `\|\| true` it — a genuine never-completing rollout must still fail. Root capacity fix: reduce warm-pool count (see [workspace-provisioning](./workspace-provisioning) Pod CPU-request sizing). |

## Related
- [Autonomous goals subsystem](./autonomous-goals)
- [Cabinet — device autonomous tab](./cabinet-device-autonomous-tab)
- [Deploy shield](./deploy-shield)
