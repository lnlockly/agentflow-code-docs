---
title: "Project Foundations — GitHub Starter Recommender"
description: "How POST /me/project-foundations/recommend ranks 24 curated GitHub templates + LLM proposals for a user's project brief"
---

# Project Foundations

## TL;DR

When a user creates a project, the backend recommends a GitHub starter
template (foundation) that matches their brief. The recommender combines
two streams: an **LLM proposer** that suggests 5 plausible repos, and a
**curated list of 24 hand-picked templates** ranked by an LLM semantic
scorer (`TEXT_SMALL`, 1 call for the whole list). Results are merged,
enriched via GitHub REST, scored on stars + recency + token overlap, and
the top 15 are returned. `POST /me/projects` defaults to `mode='github_repo'`
with the top recommendation when the client passes no `selected_foundation`.

## Files (with line refs)

| Path | Role |
|---|---|
| `agentflow-agents/src/services/curated-foundations.ts:41` | The 24-entry `CURATED_FOUNDATIONS` list + `matchesCuratedFoundation` substring matcher (fallback) |
| `agentflow-agents/src/services/foundation-llm-scorer.ts:170` | `scoreCuratedFoundations` — single LLM call ranks curated entries 0-10 for the brief, 5-min cache |
| `agentflow-agents/src/services/project-foundations.ts:406` | `recommendProjectFoundations` — orchestrates LLM proposer + LLM scorer + GitHub enrichment |
| `agentflow-agents/src/routes/projects.ts:734` | `POST /me/project-foundations/recommend` (returns up to 15 candidates) |
| `agentflow-agents/src/routes/projects.ts:815` | `POST /me/projects` default-foundation resolution (no scratch unless `ALLOW_SCRATCH_FOUNDATION=1`) |
| `agentflow-agents/tests/unit/curated-foundations.test.ts:1` | 20 matcher + integration tests |
| `agentflow-agents/tests/unit/foundation-llm-scorer.test.ts:1` | 8 scorer tests with injected LLM stub |
| `agentflow-agents/tests/unit/project-foundations-recommend.test.ts:1` | End-to-end recommend tests with injected LLM + fetch |

## Data flow

```
POST /me/project-foundations/recommend {brief, mode:"github_first", kind_hint}
  │
  ▼
recommendProjectFoundations(input)
  │
  ├─► proposeWithLlm (TEXT_LARGE)     → up to 8 LLM-proposed GitHub repos
  │
  ├─► scoreCuratedFoundations (TEXT_SMALL)
  │     │
  │     │  one call: scores all 24 curated entries 0-10 for the brief
  │     │  drops < 4, keeps top 15, cached 5min by sha1(brief|kind_hint)
  │     │
  │     └─► fallback: matchesCuratedFoundation (substring match)
  │                   on LLM-down / parse-error
  │
  ├─► merge + de-dupe by repo_full_name (lowercase)
  │
  ├─► fetchRepoMeta + fetchStackFromPackageJson  (parallel GitHub REST)
  │     │   404 = swallowed (LLM hallucinated)
  │     │
  ▼     ▼
scoreCandidate(meta, stack, tags, briefTokens)
  │   score = log10(stars+1) + recency_bonus + overlap_bonus
  ▼
sort desc, slice top 15 → 1h LRU cache → response
```

## Default foundation on POST /me/projects

```
client POSTs /me/projects {brief}            no selected_foundation
  │
  ▼
recommendProjectFoundations({brief, mode:'github_first'})
  │
  ├── pick = candidates[0]
  │   exists ? → selected_foundation = { mode:'github_repo', repo, url, ref }
  │
  └── empty result
      │
      ├── ALLOW_SCRATCH_FOUNDATION=1 → mode='scratch'
      │
      └── default                    → mode='github_repo',
                                       repo = lnlockly/golden-connect-template
```

## DB / Env / API

- **Tables:** `projects.foundation_mode`, `projects.foundation_repo_url`,
  `projects.foundation_repo_full_name`, `projects.foundation_ref`,
  `projects.foundation_snapshot` (jsonb).
- **Env:**
  - `GITHUB_TOKEN` — REST auth (optional; rate limit is the only impact).
  - `ALLOW_SCRATCH_FOUNDATION=1` — opt-in scratch fallback. Default OFF;
    we always pick `github_repo` with a fallback foundation to avoid
    empty workspace projects.
- **Routes:**
  - `POST /me/project-foundations/recommend` — wizard call.
  - `POST /me/projects` — create; resolves foundation from input or
    recommender.
- **Cache:**
  - `project-foundations.ts` — sha1(brief|mode|kind_hint), 1h TTL, 100 max.
  - `foundation-llm-scorer.ts` — sha1(brief|kind_hint), 5min TTL, 100 max.

## Curated list (24 entries, 2026-05-22)

| Niche | Repo |
|---|---|
| AgentFlow MLM | `lnlockly/golden-connect-template` |
| SaaS / Stripe | `mickasmt/next-saas-stripe-starter`, `vercel/nextjs-subscription-payments` |
| Auth-heavy | `steven-tey/precedent`, `nextauthjs/next-auth-example` |
| Content / Blog | `shadcn-ui/taxonomy`, `vercel/platforms`, `vercel/next.js` |
| E-commerce | `vercel/commerce`, `saleor/storefront` |
| AI / LLM | `mckaywrigley/chatbot-ui`, `vercel/chatbot`, `a16z-infra/companion-app`, `run-llama/create-llama` |
| Productivity | `streetwriters/notesnook`, `ueberdosis/tiptap` |
| Forms / E-sign | `formbricks/formbricks`, `documenso/documenso` |
| Booking | `calcom/cal.diy` |
| Telegram | `aiogram/aiogram`, `Telegram-Mini-Apps/tma.js` |
| Utility | `vitejs/vite`, `wallabag/wallabag` |
| Backend ref | `supabase/supabase` |

All 24 verified 200 OK on `https://api.github.com/repos/<owner>/<name>`
(2026-05-22). Redirected names resolved at verification time:
`vercel/ai-chatbot` → `vercel/chatbot`, `calcom/cal.com` → `calcom/cal.diy`.

## Workflow rule for users

The proper way to iterate on a project is:

1. POST `/me/projects` with the **core brief** ("AI mood tracker").
2. Once the project exists, send **follow-up chat messages** in the
   workspace to refine. Don't stuff every requirement into the first
   brief — the LLM scorer ranks better on short, intent-focused briefs.
3. Foundations are picked once at create-time and frozen into
   `projects.foundation_snapshot`. To swap foundation, create a new
   project (idempotency window 60s prevents accidental duplicates).

## Failure modes

| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| Recommender returns 0 candidates | `[foundations] llm returned zero suggestions for brief` | `services/project-foundations.ts:453` |
| All GitHub lookups 404 | `[foundations] all github lookups failed; returning empty` | `services/project-foundations.ts:498` |
| LLM scorer down / 5xx | `[foundations.llm-scorer] llm call failed:` | `services/foundation-llm-scorer.ts:227` |
| Scorer JSON parse error | scorer returns `[]` silently → fallback to substring | `services/foundation-llm-scorer.ts:131` |
| Project lands on `scratch` mode in DB | only when `ALLOW_SCRATCH_FOUNDATION=1` or client explicitly sent `selected_foundation.mode='scratch'` | `routes/projects.ts:815` |

## Related

- [Code-exec MCP](/subsystems/code-exec-mcp) — used by the coder agent that
  consumes the chosen foundation.
- [Foundation rebrand](/subsystems/foundation-rebrand) — how clone+rebrand
  removes upstream branding before the coder starts.
