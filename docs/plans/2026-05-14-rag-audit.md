# 2026-05-14 RAG audit — post-revolution consistency pass

## Why

Ночь shipped 20+ PR'ов (#215–#236) → docs PRs шли вслед, но
`astro.config.mjs` sidebar и `index.mdx` Code Map отстали.
Цель: signal-to-noise pass — закрыть discoverability gaps, не правя стиль.

## Findings

### A. Missing sidebar entries (5 pages exist but не видны в навигации)
- `architecture/channel-variants` — добавлен 8b66a28
- `subsystems/brief-fidelity-supervisor` — 021bdc0 (PR #223)
- `subsystems/deploy-shield` — bd498f9 (PR #228)
- `runbooks/canonical-templates` — c2788bb (PR #235)
- `runbooks/scaffold-hardening` — e192cb2 (PR #233)

### B. Code Map (index.mdx) — overview без upreffed subsystems
Текущая копия упоминает `code-exec MCP, Caddy preview, LLM router, TG MCP`.
Новые subsystems (pod-prewarm-pool, deploy-shield, brief-fidelity-supervisor,
graph-handlers, feature-flags, runtime-supervisor) не упомянуты.

### C. Consistency checks (passed)
- TL;DR: 34/34 pages have `## TL;DR` heading.
- Failure modes: 34/34 pages mention failure modes section.
- File refs spot-check: `projects.ts:2037`, `runtime-supervisor.ts:1484`,
  `tg-visitka-scaffold.ts` — все валидны.
- Pod pool: только одна страница (`subsystems/pod-prewarm-pool.mdx`),
  no duplicate page hazard.
- PR #216 scrape regex → `architecture/visitka-tg-profile.mdx` уже маркирует
  failure mode `[FIXED PR #224]` с MCP-first путём. No stale "regex" guidance.

## Changes

1. **astro.config.mjs** — добавить 5 missing entries в правильные секции.
2. **index.mdx** — расширить "What lives here" + добавить компактную таблицу
   Subsystems c slug'ами всех 12 страниц.
3. **PR**: `docs(audit): post-revolution RAG consistency pass` →
   `gh pr merge --squash --admin`.

## Out of scope
- Стилистические правки текста.
- Глубокая ревизия file:line refs (spot-check OK).
- Merge/split существующих pages.
