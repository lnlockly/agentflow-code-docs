---
title: "Runtime supervisor + preview-health gate"
description: "How peruser foundation projects stay alive after status='done' — Procfile contract, .ports.env, start_service idempotency, and the auto-pinger reconciler."
---

# Runtime supervisor + preview-health gate

## TL;DR

Every peruser foundation project (kind ∈ {spa, mini_app, landing} with a cloned Next.js boilerplate) keeps running because four invariants hold at all times:

1. `/workspace/<slug>/.ports.env` exists with `PORT=<n>`.
2. `/workspace/<slug>/Procfile` sources `.ports.env` on every supervisor restart.
3. `/workspace/<slug>/.services/web/pid` points at the live web process — adopted from a running PID when missing.
4. `start_service` is idempotent — it returns `already_running` instead of relaunching when (a) the pidfile points at a live PID and (b) any declared port is bound.

Before 2026-05-22 these invariants drifted for legacy projects (Monar / 1292), causing EADDRINUSE relaunch loops, log spam, and preview-503s when `$PORT` expanded empty after a clean-env restart. The reconciler ticker now adopts legacy projects into the contract within ~10 minutes of agent boot.

## Files (with line refs)

| Path | Role |
|---|---|
| `agentflow-agents/src/services/runtime-supervisor.ts:535-555` | Next.js runtime branch (PR #730, 2026-05-22). Detects `next` in `dependencies`/`devDependencies` BEFORE the generic build-script branch and emits `type='nextjs'` with `npx next start -H 0.0.0.0 -p <foundation_port>`. Bound to `computePortBase(projectId).port`. |
| `agentflow-agents/src/services/runtime-supervisor.ts:1452-1470` | `processesForPlan` — nextjs lands as service `name='web'` (Heroku convention) so downstream tooling and the Caddy preview sidecar pick it up. |
| `agentflow-agents/src/services/pool-worker/post-build.ts:599-614` | Always-on `deploy()` bucket now includes `'nextjs'` — fresh Next.js projects boot without `RUNTIME_SUPERVISOR_ENABLED=1`. |
| `agentflow-agents/src/services/runtime-supervisor-health.ts:332-340` | `healthcheckByType` routes `nextjs` through `probeApi` (HTTP probe on `plan.port`). |
| `agentflow-agents/src/services/graph-executor/handlers/spa.ts:798-833` | Foundation bridge: writes `.ports.env`, generates the Procfile with the `${PORTS_SRC}` source line in every variant. |
| `agentflow-agents/src/mcp-servers/code-exec/index.ts:1899-2240` | `start_service` MCP tool: idempotency probe + force mode. |
| `agentflow-agents/src/services/pingers/peruser-services-reconcile.ts:1-260` | Auto-pinger reconciler: writes missing `.ports.env`, patches legacy Procfiles, adopts the running PID. |
| `agentflow-agents/src/services/auto-pinger.ts:151-175` | Wires the reconciler into the 10-minute tick. Boot-tick fires once 5 s after startup. |
| `agentflow-agents/src/services/peruser-pod.ts:308-353` | `renderCreateProjectScript` — the bootstrap path that writes `.ports.env` for new projects. |
| `agentflow-agents/src/services/peruser-pod.ts:69-83` | `computePortBase(projectId)` — deterministic 4-port window per project (PORT/BOT_PORT/VITE_PORT/PREVIEW_PORT). |
| `agentflow-agents/src/services/pingers/preview-health.ts:88-192` | `runPreviewHealthTick` — terminal-project HEAD probe + `detectAndRepointPreview` reanimation, 5-min per-project cooldown. |
| `agentflow-agents/src/services/pingers/preview-placeholder.ts:189-214` | `shouldHoldFinalizeForPlaceholder` — preview-gate predicate the graph-executor `runProjectExecutionBackground` uses before flipping `status='done'`. |
| `agentflow-agents/src/services/projects.ts:3098-3175` | The pre-`done` preview gate that calls `probeAndGatePreview` with backoff (0/5/15/30 s) and flips to `failed` on persistent placeholder, missing brief tokens, or recent build_failed. |
| `agentflow-agents/src/services/pingers/preview-health-strict.ts:1-280` | Strict-gate orchestrator: AgentFlow placeholder + brief-token + recent build_failed checks. Pure functions injectable in tests. |
| `agentflow-agents/src/services/pingers/preview-placeholder.ts:extractBriefTokens` | Lowercases, stopword-filters, dedupes brief into ≤12 distinctive tokens (len≥4). |
| `agentflow-agents/tests/unit/preview-health-strict.test.ts` | 26 unit tests pinning every layer + the Russian reason map. |

## Data flow

```
            ┌─────────────────────────┐
 approve →  │ runProjectExecutionBg   │
            │  → graph-executor walk  │
            └────────────┬────────────┘
                         │  every node done
                         ▼
            ┌─────────────────────────┐
            │ preview-gate (4 probes) │ ← shouldHoldFinalizeForPlaceholder
            └────────────┬────────────┘
              hold=false │            │ hold=true
                         ▼            ▼
                  status='done'  status='failed' + preview_gate_blocked
                         │
                         ▼
            ┌─────────────────────────┐
            │ runtime-supervisor      │  Procfile → start_service(name=web, port=N)
            │  (auto-pinger)          │     ↓ idempotency probe
            │                         │     ↓ if PID alive && port bound → no-op
            └────────────┬────────────┘
                         │
                         ▼
            ┌─────────────────────────┐
            │ preview-health watchdog │  HEAD probe terminal preview URLs every 2 min
            │  (auto-pinger)          │  on 5xx / placeholder → detectAndRepointPreview
            └─────────────────────────┘

  Legacy projects (Monar et al.) lacking .ports.env / .services/web/pid:
            ┌─────────────────────────────────────┐
            │ peruser-services-reconcile (10 min) │
            │  - writes .ports.env from           │
            │    computePortBase(projectId)       │
            │  - patches Procfile to source it    │
            │  - adopts running PID via           │
            │    lsof | ss | /proc/net/tcp        │
            └─────────────────────────────────────┘
```

## DB / Env / API

- **Tables:** `projects` (kind, status, slug, workspace_project_name, owner_user_id, preview_url, foundation_mode), `project_events` (kind='log', 'preview_gate_blocked', 'preview_health_failed', 'preview_dead_3x', 'peruser_services_reconcile', payload source='auto-pinger' / 'graph-executor-finish').
- **Env:** `PERUSER_POD_ENABLED_USER_IDS` (CSV of user IDs routed through the peruser pod model), `FF_BOT_AUTO_TEST`, `FF_SELF_HEAL_LOOP`.
- **MCP tools:** `start_service` (new params `port`, `mode` ∈ `idempotent`|`force`), `stop_service`, `list_services`.
- **Port window per project:** `30_000 + projectId * 100 + {0,1,2,3}` (PORT/BOT_PORT/VITE_PORT/PREVIEW_PORT).

## Failure modes

| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| `next start -p $PORT` fails with `Invalid port: 0` after auto-pinger restart | `Invalid port` in `.services/web/log` | `services/graph-executor/handlers/spa.ts:798-833` — Procfile must contain `${PORTS_SRC}` |
| EADDRINUSE relaunch loop in pinger logs | `start_service` called repeatedly with same name and no `already_running` echo | `mcp-servers/code-exec/index.ts:2108-2186` — idempotency probe must run before launch |
| Preview returns 502 but no `peruser_services_reconcile` event | (no reconcile event in `project_events`) | `services/pingers/peruser-services-reconcile.ts` — verify ticker is wired in `auto-pinger.ts` |
| `preview_dead_3x` event without follow-up `repoint_ok` | `preview_dead_3x:` in `project_events` | `services/pingers/preview-health.ts:175-185` + `services/pool-worker/post-build.ts:1505` (`detectAndRepointPreview`) |
| `.ports.env` missing for an existing peruser project | `peruser_services_reconcile: ports_written=1` event appears | `services/pingers/peruser-services-reconcile.ts:renderReconcileScript` |
| Graph flips `status='done'` while preview serves Caddy stub | `preview_gate_blocked` event | `services/projects.ts:3098-3151` — backoff retry already covers this |
| Graph flips `status='done'` while preview serves AgentFlow placeholder (`Запускаем проект…`) | `preview_health_failed: placeholder_served` | `services/projects.ts:3098-3151` + `services/pingers/preview-health-strict.ts:probeAndGatePreview` |
| Graph flips `status='done'` but DOM has zero brief-derived tokens | `preview_health_failed: no_brief_tokens_in_dom` | `services/pingers/preview-placeholder.ts:classifyPreviewProbe` layer 4 (brief-token check) |
| `build_failed` event in last 3 min but project still shipped `done` | `preview_health_failed: build_failed_recent` | `services/pingers/preview-health-strict.ts:inspectBuildFailedEvents` — extend `BUILD_RETRY_SUCCESS_KINDS` if a new success-event is added |
| Preview HTTP=200 but real status=502 from upstream | `preview_health_failed: http_not_200` | upstream pod down — check `kubectl -n af-tenant-<u> logs project-proj-<slug>-0` |
| Fresh Next.js project serves AgentFlow placeholder forever | classify reason was `package_json_build_script` (legacy `type='web'` skip) — fixed by `nextjs_detected` branch | `runtime-supervisor.ts:535-555` — check `package.json.dependencies.next` exists; verify `processesForPlan` emits the `web` service with `npx next start -p <foundation_port>` |

## Strict preview-health gate (PR agentflow-agents#729, 2026-05-22)

Before flipping `projects.status='done'` the graph-executor finalize path
in `services/projects.ts:3098-3151` runs `probeAndGatePreview` from
`services/pingers/preview-health-strict.ts`. Decision tree, in order:

1. **AgentFlow placeholder title** — `<title>` contains either
   (`Запускаем` OR `Workspace warming up` OR `Coming soon`) AND `AgentFlow`,
   in either order. Catches the ingress fallback page Caddy serves when the
   per-project preview pod has no usable upstream.
2. **Caddy `Workspace ready` stub** — legacy layer, still active.
3. **Tiny body** — `< TINY_BODY_BYTES` (800) without an SPA-shell marker.
4. **SHA-256 deny-list** — `src/config/placeholder-hashes.json` hashes.
5. **Brief-token check** — when the brief produces ≥3 distinctive tokens
   after stopword + len≥4 filtering, the DOM must contain ≥1 of them
   (substring, case-insensitive). Briefs with fewer tokens skip the layer
   (no signal). Algorithm in `services/pingers/preview-placeholder.ts:extractBriefTokens`.
6. **Recent build_failed event** — `project_events` query for `build_failed`
   in the last 180 s with no `build_succeeded` / `deploy_succeeded` /
   `preview_repointed` event after it.

On any hold, the gate emits THREE events into `project_events`:
- `preview_health_failed` with full `diagnostic` JSON (check, http_status,
  body_bytes, dom_sample, brief_tokens_checked, brief_tokens_found,
  latest_build_failed_at).
- `preview_gate_blocked` for back-compat with the legacy workspace UI.
- `log` with a Russian-language reason (`strictGateReasonRu`) so the owner
  sees the failure in the timeline feed.

Status is flipped to `failed`, NOT `done`. `preview_ready_final` is NOT
emitted. `runGraphExecutorPostDoneHooks` is NOT called.

Greppable reasons (used to triage in `kubectl logs deploy/agentflow-agents | grep preview_health_failed`):
- `placeholder_served` (alias group covering `caddy_workspace_ready_title`,
  `agentflow_placeholder_title`, `placeholder_hash_match`, `tiny_body`).
- `no_brief_tokens_in_dom`.
- `build_failed_recent`.
- `http_not_200` / `unreachable` / `non_200_status`.

## Next.js runtime detection (PR agentflow-agents#730, 2026-05-22)

The default AgentFlow project stack is Next.js. Before this PR, `classify`
saw `package.json.scripts.build = "next build"`, took the generic
build-script branch, returned `type='web'` with `port=8080`, and
`writeS6Services` short-circuited — no process bound the per-project
foundation port, the Caddy preview sidecar fell back to the
`<title>Запускаем проект… · AgentFlow</title>` placeholder, and every
fresh Next.js project (#1373..#1381 on 2026-05-22) shipped broken.

Detection rule in `classify` (priority order, FIRST match wins):

1. Fullstack Node + Procfile / multi-process — unchanged.
2. `/workspace/dist/index.html` present → `type='static'`. Preserves static
   exports (`next export`, Vite production builds, etc.) — the Caddy
   sidecar serves the prebuilt artefact and no `next start` runs.
3. **NEW: `package.json.dependencies.next` OR `package.json.devDependencies.next`**
   → `type='nextjs'`, `reason='nextjs_detected'`, port =
   `computePortBase(projectId).port`. Emits a single process named `web`
   running `[ -f .ports.env ] && . ./.ports.env; exec npx next start -H 0.0.0.0 -p "${PORT:-<port>}"`.
4. Generic `package.json.scripts.build` → `type='web'` (Vite/CRA/etc.).
5. Python branches — unchanged.

`writeS6Services` allow-list now passes `nextjs` through to the
`start_service` loop (the static-skip branch at L1577 covers only
`static`/`web`/`unknown`). `pool-worker/post-build.ts` always-on bucket
includes `'nextjs'` so the boot fires without the
`RUNTIME_SUPERVISOR_ENABLED` flag.

Greppable detection signal for postmortems:
- `runtime_detect_shadow` payload contains `"reason":"nextjs_detected"`.
- `runtime_supervisor_deployed` payload `plan.type` = `"nextjs"`.
- `start_service` event with `name='web'` and `port=<computePortBase(projectId).port>`.

## Reconcile contract

`runPeruserServicesReconcileTick` is **read-mostly** — never restarts the running web process. Its single mutation is to fill in missing platform metadata:

- `.ports.env` write guarded by `[ ! -f .ports.env ]`.
- `Procfile` rewrite guarded by `! grep -q '[ -f .ports.env ]' Procfile` (sentinel skip when bridge-generated Procfile already has it).
- PID adoption guarded by `[ ! -s .services/web/pid ]` AND `kill -0 "$CANDIDATE"` on the discovered PID.

The script emits one machine-parseable line — `RECONCILE: ports=<0|1> proc=<0|1> pid=<n|none>` — so the agent-side caller records a single structured `project_events.kind='log'` entry without re-grepping stdout.

PID discovery cascade: `lsof -t -i TCP:$PORT -sTCP:LISTEN` → `ss -lntp` (parse `pid=N`) → `/proc/net/tcp` (find LISTEN inode, follow `/proc/<pid>/fd/*` symlinks). The third path is the only one that works in a stripped Alpine-derived workspace image where neither `lsof` nor `ss` ship.

Per-project cooldown: 10 min between attempts. On first success the project lands in the `succeeded: true` slot of the in-memory state map and is skipped forever (until agent restart).

## start_service contract

```ts
{
  project: string,    // peruser slug
  name: string,       // service name (e.g. 'web')
  command: string,    // foreman-style web: line, NOT a full bash script
  cwd?: string,
  port?: number,      // optional — required for the port-bound idempotency check
  mode?: 'idempotent' | 'force', // default 'idempotent'
}
```

**Idempotency probe** runs inside the workspace pod before composing the launch script:

```sh
if [ -f .services/<name>/pid ]; then
  PID=$(cat .services/<name>/pid)
  if [ -n "$PID" ] && kill -0 "$PID" && ( ss -tln | grep ":<port>" || netstat -lnt | grep ":<port>" ); then
    echo "ALIVE:$PID"
  else
    echo "DEAD"
  fi
else
  echo "NOPID"
fi
```

Returns `{ ok: true, status: 'already_running', pid, port }` on `ALIVE:` — no kill, no relaunch. `mode: 'force'` skips the probe and always restarts; use it for code-redeploy / config-change paths.

## Related

- [graph-executor](./graph-executor.mdx) — owns the node DAG that runs before `status='done'`.
- [computer-mcp](./computer-mcp.mdx) — sibling pattern: idempotent service launch over MCP.
- `agentflow-agents/docs/specs/2026-05-22-runtime-supervisor-bulletproof.md` — spec for this PR.
