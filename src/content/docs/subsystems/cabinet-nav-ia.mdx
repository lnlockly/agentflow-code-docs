---
title: "Cabinet navigation IA (core-5 + Ещё disclosure)"
description: "How the authenticated cabinet sidebar + mobile bottom bar are structured: 5 core surfaces first, everything secondary tucked behind a collapsible group."
---

# Cabinet navigation IA

## TL;DR

The authenticated cabinet nav leads with **4 core surfaces** — Проекты,
**Агенты** (`/cabinet/agents` hub, A2), Маркетплейс, **Баланс и AI**
(renamed from «LLM-кабинет», Фаза 1) — under an «Основное» group, with a
pinned «Главная» home above it. **Приложение** (desktop agent) and
**Облако** (24/7) moved out of core into «Ещё» in A2 — they remain the
device substrate the «Агенты» hub reads from, just no longer top-level.
Everything else (мои агенты, flows, device skills, the `/apps` launcher,
billing, settings) is tucked behind a collapsible «Ещё» disclosure that
defaults closed. **Referrals live in exactly one place — the avatar
dropdown** (de-duplicated in Фаза 1). All nav lives in one component;
`/apps` is the overflow launcher and the avatar dropdown carries
profile/referrals/settings/billing/logout.

The **«Агенты» hub** (`/cabinet/agents`) is two stacked sections: a static
catalog of 6 use-case scenarios (filter-chips by category, each card →
`/cabinet/agents/new?kind=<key>`) and «Твои агенты» — the user's running
agents, reusing the exact device data path from `CabinetDevices`
(`GET /me/devices` + `GET /me/hosted-devices`). `/cabinet/agents/new` is a
real 3-step scenario setup wizard (A2b — replaced the «Мастер скоро» stub):
**1. Что будет делать** (scenario pre-selected from `?kind`, brief textarea),
**2. Где + как часто** (cloud 24/7 recommended / Mac local + schedule chip),
**3. Контроль** (approval toggle + daily spend cap). Launch is real: cloud
posts `/me/hosted-devices` (daemon) + applies the control step as a
`DeviceScope` via `devicesApi.updateScope`, then navigates to the live device
page carrying the brief as `?task=`; a 404 degrades to a hand-off to the full
cloud wizard. Mac hands off to the local installer at `/cabinet/devices/new`.

## Files (with line refs)

| Path | Role |
|---|---|
| `agentflow-landing/src/components/layout/AppShellV2.tsx:128` | `CORE_NAV` (4 items: projects, agents, marketplace, llm_cabinet) |
| `agentflow-landing/src/components/layout/AppShellV2.tsx:147` | `MORE_NAV` (now leads with application + cloud) |
| `agentflow-landing/src/components/layout/AppShellV2.tsx:187` | `BOTTOM_NAV` (mobile 5-tab bar — «Агенты» replaced «Приложение») |
| `agentflow-landing/src/components/layout/AppShellV2.tsx:163` | `ADMIN_NAV` (shown only on `/admin/*` for `is_admin`) |
| `agentflow-landing/src/pages/CabinetAgentsHub.tsx:47` | `/cabinet/agents` hub — imports `AGENT_SCENARIOS` catalog + «Твои агенты» (reuses device queries) |
| `agentflow-landing/src/pages/CabinetAgentNew.tsx` | `/cabinet/agents/new` real 3-step scenario wizard — cloud create via `/me/hosted-devices` + scope, or hand-off to local installer |
| `agentflow-landing/src/lib/agentScenarios.ts` | shared scenario catalog (`AGENT_SCENARIOS`, `SCENARIO_CATEGORIES`, `findScenario`) — single source for hub + wizard |
| `agentflow-landing/src/main.tsx:726` | `/cabinet/agents` + `/cabinet/agents/new` routes (old redirect to `/account/agents` removed) |
| `agentflow-landing/src/styles/cabinet-agents.css` | hub styles (mobile-first; `!important` beats the 36px button floor for ≥44px taps) |
| `agentflow-landing/src/pages/AppsLauncher.tsx:83` | `/apps` overflow grid (`SECTIONS`) |
| `agentflow-landing/src/i18n/{ru,en,zh}.json` | `app.shell.nav.{agents,my_agents,application,cloud,llm_cabinet,more}`, `cabinet.agents.*` |

## Data flow

```
AppShellV2 (every cabinet route)
  ├─ Главная            /cabinet                       (pinned, HOME_ITEM)
  ├─ «Основное» (CORE_NAV, always open — 4 items, A2)
  │   ├─ Проекты        /projects
  │   ├─ Агенты         /cabinet/agents  (hub: catalog + «Твои агенты»)
  │   ├─ Маркетплейс    /flows
  │   └─ Баланс и AI    /llm-cabinet     (renamed from «LLM-кабинет», Фаза 1)
  ├─ «Ещё» ▸ (MORE_NAV, collapsed by default, persisted)
  │   ├─ Приложение     /cabinet/devices              (moved from core, A2)
  │   ├─ Облако         /cabinet/devices/hosted/new   (moved from core, A2)
  │   ├─ Мои агенты     /account/agents   (key `my_agents` — distinct from core «Агенты»)
  │   ├─ Нанять агента  /marketplace
  │   ├─ Сценарии       /my/flows
  │   ├─ Навыки         /cabinet/devices/skills
  │   ├─ Приложения     /apps          ── overflow launcher (AppsLauncher)
  │   ├─ Биллинг        /account/usage   (also in avatar menu)
  │   └─ Настройки      /account/settings (also in avatar menu)
  ├─ «Десктоп-агент»   GitHub release download links
  └─ avatar dropdown   Profile · Рефералы · Settings · Billing · [Admin] · Logout
                       (referrals live ONLY here now — Фаза 1 de-dup)

/cabinet/agents (CabinetAgentsHub)
  ├─ Каталог: 6 UseCase cards + category filter chips → /cabinet/agents/new?kind=<key>
  └─ «Твои агенты»: GET /me/devices + GET /me/hosted-devices (same path as CabinetDevices)
/cabinet/agents/new (CabinetAgentNew) — 3-step scenario wizard
  ├─ 1. Что будет делать  (scenario from ?kind + brief textarea)
  ├─ 2. Где + как часто    (cloud daemon / Mac local + schedule chip)
  ├─ 3. Контроль          (approval → confirm_before, cap → budget_usd)
  └─ Запустить → cloud: POST /me/hosted-devices + updateScope → /cabinet/devices/hosted/<id>?task=
                 (404 → hand-off /cabinet/devices/hosted/new) · Mac → /cabinet/devices/new

mobile bottom bar (BOTTOM_NAV, 5): Главная · Проекты · Агенты · Маркетплейс · Ещё(/apps)
admin: on /admin/* AND is_admin → ADMIN_NAV replaces the workspace groups
```

## DB / Env / API

- Tables: none (pure client IA).
- Env: none.
- Routes referenced (all `RequireAuth` + `AccessGate`): `/cabinet`,
  `/projects`, `/cabinet/agents`, `/cabinet/agents/new`, `/flows`,
  `/llm-cabinet`, `/cabinet/devices`, `/cabinet/devices/hosted/new`,
  `/account/agents`, `/marketplace`, `/my/flows`, `/cabinet/devices/skills`,
  `/apps`, `/account/{referrals,usage,settings}`.
- APIs (hub «Твои агенты»): `GET /me/devices`, `GET /me/hosted-devices`.
- localStorage: `agentflow.sidebar.more-open` (disclosure state).

## Failure modes

| Symptom | Log signature for grep | Where to fix |
|---|---|---|
| Core surface missing from sidebar | `CORE_NAV` (grep the array) | `AppShellV2.tsx:110` — add the `NavItem` |
| «Ещё» won't expand | `af-sb__section-toggle` | `AppShellV2.tsx` toggle button + `dark-tokens.css:149` |
| Nav label shows raw key (`app.shell.nav.x`) | missing i18n key | add to all three of `src/i18n/{ru,en,zh}.json` |
| Cut feature reappears in nav (souls / квесты / singularity) | `souls` / `quests` / `Singularity` in a NAV array | keep them out of `CORE_NAV`/`MORE_NAV` (removed in landing #545/#547) |
| `/cabinet/agents` redirects to `/account/agents` instead of the hub | duplicate `<Route path="/cabinet/agents" …Navigate…>` | remove the stale redirect in `main.tsx` — the hub route must be the only one |
| Core «Агенты» and «Мои агенты» both show «Агенты» | both NavItems use key `agents` | core uses `agents`, MORE_NAV uses `my_agents` — keep the keys distinct |
| Hub tap targets render at 36px on mobile | `.af-shell-v2 button { min-height: 36px }` floor wins | `cabinet-agents.css` uses `min-height: 44px !important` on chips/refresh |
| Deprecated surface re-surfaces in cabinet (farm / inbox) | `to="/farm"` or `to="/inbox"` in a nav/home/apps array | keep entry points out of `HomeWidgetsRow.tsx`, `AppShellV2.tsx` topbar, `AppsLauncher.tsx` (removed in landing #566) |
| Home Balance card renders half-width | `.pdp .pd-widgets-row` not `grid-template-columns:1fr` | `cabinet-dashboard.css` — single-hero grid after the marketing card removal |

## History

- **A2b scenario wizard** (2026-05-31, landing PR #571) — replaced the
  `/cabinet/agents/new` «Мастер скоро» stub with a real 3-step setup
  (что будет делать → где + как часто → контроль). Cloud launch creates a
  daemon via `/me/hosted-devices` + applies the control step as a
  `DeviceScope` (`updateScope`), then navigates to the live device with the
  brief as `?task=`; 404 hands off to the full cloud wizard, Mac hands off to
  `/cabinet/devices/new`. Catalog extracted to `lib/agentScenarios.ts`
  (shared by hub + wizard). Mobile-first verified at 390/360px.
- landing #545/#547 — first declutter pass: removed Singularity / Души /
  Квесты / legacy marketplace dupe; regrouped into four outcome groups.
- Category H `h-nav-declutter` (2026-05-30) — collapsed the four groups into
  «Основное» (the core 5) + a single collapsible «Ещё», promoted the
  previously hidden `/llm-cabinet` into the primary nav, surfaced Cloud as a
  distinct entry. Map: `plans/autonomous/_cat-h-ia-map.md`.
- Cabinet redesign **Фаза 1** (2026-05-30, landing PR #558) — renamed
  «LLM-кабинет» → «Баланс и AI» (`app.shell.nav.llm_cabinet`, `apps.tile.llm_cabinet`,
  `CabinetTopNav` subtitle, `LlmCabinet` document.title); removed the duplicate
  «Рефералы» tab from `LlmCabinetShell` and the `MORE_NAV` row, leaving referrals
  only in the avatar dropdown (`app.shell.menu.referrals`); decluttered the
  `/cabinet` home (quick actions 9→4, stat tiles 4→2, referrals widget dropped).
  Concept: `agentflow/CABINET-CONCEPT.html`.
- **A2 «Агенты» hub** (2026-05-30, landing PR #564) — new `/cabinet/agents`
  hub (scenario catalog + «Твои агенты», reusing the device queries) plus a
  `/cabinet/agents/new` wizard placeholder. `CORE_NAV` cut to 4 (Проекты,
  Агенты, Маркетплейс, Баланс и AI); «Приложение» + «Облако» moved into «Ещё»;
  the mobile bottom-bar «Приложение» tab became «Агенты». New i18n
  `app.shell.nav.agents` (= «Агенты») with the old `/account/agents` entry
  renamed to `my_agents`, plus the `cabinet.agents.*` set. Removed the stale
  `/cabinet/agents → /account/agents` redirect. Wizard + autoposting are a
  follow-up. Concept: `agentflow/CABINET-CONCEPT.html` §5.2.
- **Deprecation sweep** (2026-05-30, landing PR #566) — pulled cabinet entry
  points to surfaces that are not in the concept. Removed: the home
  «Маркетинг»/ферма card (`MarketingWidget` in `HomeWidgetsRow.tsx` → `/farm`),
  leaving **«Баланс» as a single full-width hero** (`.pdp .pd-widgets-row`
  switched to `grid-template-columns:1fr`); the topbar **Bell → `/inbox`**
  button in `AppShellV2.tsx`; the `/inbox` and `/farm` tiles in
  `AppsLauncher.tsx`. Orphaned i18n `projects.dashboard.widget.marketing.*`
  (ru/en/zh) + unused icon imports (`Bell`, `Sprout`, `Inbox`, `Tractor`)
  removed. Routes/pages left intact — only the cabinet entry points are gone;
  a deeper page/route cleanup is a separate follow-up. Owner left as ASK (not
  removed): `my_agents` (`/account/agents`), `flows` (`/my/flows`),
  `device_skills` (`/cabinet/devices/skills`), `hire_agents` (`/marketplace`),
  `apps` (`/apps`), `/quests`. Concept: `agentflow/CABINET-CONCEPT.html` §1.1.

## Related
- [Devices — unified routing](/subsystems/devices-unified)
- [LLM cabinet frontend](/subsystems/llm-cabinet-frontend)
- [Projects create flow](/subsystems/projects-create-flow)
